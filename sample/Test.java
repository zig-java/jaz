package sample;

/**
 * Compile me with Java 16!
 */
public class Test {
    public int sign(int a) {
        if(a<0) return -1;
        else if (a>0) return 1;
        else return 0;
    }
}
