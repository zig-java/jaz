package sample;

/**
 * Compile me with Java 16!
 */
public class Test {
    public static int sign(int a, int b) {
        if (a > b) return -1;
        else if (a == 1) return 1;
        else if (b == 1) return 0;
        return 10;
    }
}
